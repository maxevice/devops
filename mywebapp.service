[Unit]
Description=My WebApp Docker Container
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
Restart=always
ExecStartPre=-/usr/bin/docker stop mywebapp
ExecStartPre=-/usr/bin/docker rm mywebapp
ExecStart=/usr/bin/docker run --name mywebapp --network host ghcr.io/maxevice/devops:stable
ExecStop=/usr/bin/docker stop mywebapp

[Install]
WantedBy=multi-user.target